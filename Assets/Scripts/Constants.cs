using UnityEngine;

namespace Constants {
    public class Constants
    {

        public enum SFXType
        {
            None,
            HitEnemy,
            DeadPlayer,
            PLayerWin,
            TreasureSpawn,
            ClockTick
        }

        public enum BackgroundMusicType
        { 
            LavaFLowing,
            BackgroundMusic
        }
    }
}